# ============================================================
# SHAHEEN-YS — Backend Python Dependencies
#
# This file is used by backend/Dockerfile.
# It mirrors the packages declared in source/web_service/pyproject.toml
# so Docker can install them without Poetry inside the container.
#
# Update: run `poetry export -f requirements.txt --without-hashes > backend/requirements.txt`
#         from source/web_service/ to regenerate from pyproject.toml.
# ============================================================

# ── Django core ──────────────────────────────────────────────
Django[async]>=5.2.6,<6.0
djangorestframework>=3.16.1,<4.0
drf-spectacular>=0.28.0,<1.0

# ── ASGI server ──────────────────────────────────────────────
daphne>=4.2.1,<5.0
channels>=4.3.1,<5.0
channels-redis>=4.3.0,<5.0
gunicorn>=23.0.0,<24.0
uvicorn>=0.35.0,<1.0

# ── Database ─────────────────────────────────────────────────
psycopg2-binary>=2.9.10,<3.0

# ── Cache / broker ───────────────────────────────────────────
redis>=6.4.0,<7.0

# ── Celery (async task queue) ─────────────────────────────────
celery>=5.4.0,<6.0
django-celery-beat>=2.7.0,<3.0
django-celery-results>=2.5.0,<3.0

# ── HTTP clients ─────────────────────────────────────────────
requests>=2.32.5,<3.0
httpx>=0.28.1,<1.0
websockets>=15.0.1,<16.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0
openai>=1.107.0,<2.0
whitenoise[brotli]>=6.9.0,<7.0

# ── Web scraping (used by AI agent) ───────────────────────────
beautifulsoup4>=4.12.0,<5.0
# ddgs is the package name used in pyproject.toml and imported as `ddgs` at runtime
ddgs>=9.6.0

# ── Static files / storage ────────────────────────────────────
Pillow>=10.4.0,<11.0
